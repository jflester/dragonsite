<% provide(:title, 'All courses') %>
<h1>All courses</h1>

<%= will_paginate %>

<ul class="courses">
  <% @courses.each do |course| %>
    <%= render course %>
  <% end %>
</ul>

<%= will_paginate %>
